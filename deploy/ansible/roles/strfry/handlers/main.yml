---
- name: Reload systemd for strfry
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart strfry
  ansible.builtin.systemd_service:
    name: strfry.service
    state: restarted
  when: totem_service_restarts_enabled | bool
