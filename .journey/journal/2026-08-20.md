# 2026-08-20

## E-ink Rev 2.1 compatibility and final render

Resumed the Waveshare 2.13-inch display work after the Pi Zero returned
online. The user supplied decisive control evidence: the same assembled
Pi/HAT/panel refreshes successfully when booted from a Pwnagotchi SD card.
That rules out HAT/FPC seating and panel failure.

Compared Totem's V2 transport with Pwnagotchi's current `waveshare_2`
implementation. Pwnagotchi configures GPIO24/BUSY with an internal pull-down
and does not require observing a BUSY leading edge before accepting a V2
refresh. Totem previously used no pull resistor and made that leading edge a
hard requirement. On this Rev 2.1 HAT, GPIO24 remains electrically floating
at idle (it follows both software pull-up and pull-down) and no leading edge
is visible even though the display itself is known to work.

Changes:

- bias active-high BUSY low, matching Waveshare/Pwnagotchi Raspberry Pi
  transport behavior;
- make the V2/Rev 2.1 update path wait on BUSY if asserted without requiring
  the leading edge;
- keep the controller powered for a four-second fallback refresh window
  before callers may put it to sleep;
- add contract coverage for the pull-down and V2 behavior.

Verification:

- full local suite: 74 passed, 4 hardware-marked tests skipped;
- deployed the changed base and V2 drivers to `/opt/totem`;
- invoked `examples/display_text.py` through
  `DisplayManager(driver_name="waveshare_2in13_v2")` on real SPI with the
  exact payload `hello world\n<3 totem`;
- initialization, framebuffer transfer, protected refresh interval, sleep,
  and cleanup all completed with exit status 0.

Physical visual confirmation of the exact final text is still pending from
the person beside the screen; do not call the display goal complete without
that last observation.

## Correction: physical target and controller identity

The preceding diagnosis was based on the wrong physical unit. The display,
camera view, and PiSugar2 are attached to `metot` (`192.168.8.239`), while the
failed V1/V2/V3/V4 and BUSY-line experiments targeted screenless `totem`
(`192.168.8.136`). The floating GPIO24 readings therefore described an open
Pi header, not a display fault. Do not carry those readings forward as HAT
behavior.

`Rev 2.1` is the HAT PCB revision and does not identify the e-paper controller.
On `metot`, the official Waveshare V4 initialization/frame path immediately
rendered `hello world\n<3 totem`; the user confirmed the result and the camera
recorded it. This identifies the attached controller as V4.

The working setup was then promoted into the repository and deployed from that
source state:

- `DisplayManager` now selects `TOTEM_EINK_DRIVER` when no explicit driver is
  supplied, so the API service uses the same configuration path as direct
  manager callers;
- `deploy/devices/metot.env` pins `waveshare_2in13_v4`, and the corresponding
  boot fragment records persistent SPI0 enablement;
- the Raspberry Pi transport prefers Waveshare's gpiozero-style GPIO backend;
- V4 retains power for a bounded refresh-settle interval before callers may
  sleep or close it;
- speculative V2/Rev-2.1 BUSY conclusions were removed from the source and
  tests.

Verification:

- local suite: 81 tests passed, 4 hardware tests skipped;
- the source-built `totem.service` is active on `metot`, has
  `TOTEM_EINK_DRIVER=waveshare_2in13_v4`, and reports the gpiozero/V4 driver at
  250x122;
- SPI is live at `/dev/spidev0.0` and `dtparam=spi=on` is persisted;
- an API call through `DisplayManager` visibly changed the retained image to
  `MANAGER\nBASELINE` under camera observation;
- a second recorded API call changed it back to
  `hello world\n<3 totem`, proving a fresh manager-driven refresh rather than
  relying on the earlier retained e-ink image.
- hardened V4 refresh handling then observed the panel's real BUSY
  assertion/release (no fallback delay), and a final recorded manager call
  again rendered `hello world\n<3 totem` from the source-built service;
- BUSY confirmation is now an explicit Waveshare 2.13-inch driver capability,
  so hardware smoke tests enforce it without imposing that signal contract on
  unrelated display drivers.

## Image and raw-frame example validation

Added `examples/display_image.py` and `examples/display_bytes.py` in commit
`61d9e3b`, documented both commands, and validated the exact PR commit on
`metot` with the production service stopped to guarantee exclusive GPIO/SPI
ownership.

- the image example decoded a generated 250x122 monochrome PNG and visibly
  rendered `IMAGE\nOK` inside its border;
- the byte example read a controller-native 4,000-byte framebuffer and visibly
  rendered `BYTES\nOK` inside its border;
- both commands exited successfully and completed a real BUSY cycle without
  the conservative fallback delay;
- separate camera recordings captured both refreshes and final screen states;
- `totem.service` was restarted afterward, its V4 environment was verified,
  and `hello world\n<3 totem` was restored through the production API;
- local tests remained at 81 passed and 4 hardware tests skipped; GitHub's
  Python 3.9 and 3.12 mock-transport jobs passed.
## motown onboarding (test unit #3)

Fresh RPi flashed and joined to the LAN.

- Found via mDNS: `motown.local` → `192.168.8.196` (getent; address is
  DHCP, mDNS name is the stable handle).
- Image: Debian 13 (trixie), aarch64 — note this is a 64-bit unit,
  unlike totem (armv6l). Anything we cross-build per-device (e.g. the
  nak armv6 build from 2026-08-19) needs an aarch64 variant here.
- `~/.ssh/config`: added alias `motown` (HostName `motown.local`,
  User `totem`), same pattern as totem/metot.
- Key onboarding: no `sshpass` on the dev host; `ssh-copy-id` driven
  with pexpect (password `totem`). Passwordless verified.
- AGENTS.md updated with a motown section.

### Loose ends / next steps

- Identify the mystery peers (`npub1qrjr...msuc`, `npub10fu6...2cjf`).

## motown fips bring-up (aarch64)

Built fips from `references/fips` HEAD `23ec0a7` for motown — same
commit as totem's running binary (`fips 0.5.0-dev rev 23ec0a7b81`).

### Build recipe: aarch64 static musl cross (docker)

Ladder notes, in order tried:

1. Upstream release artifacts are for tagged releases only — our mesh
   runs the dev checkout; mixing versions in a mesh is a gamble. Build
   it ourselves.
2. **gnu target blocked**: BLE transport auto-enables on non-musl
   linux (`build.rs` `bluer_available` — no opt-out env/feature), and
   `libdbus-sys` refuses to cross-compile without a target pkg-config
   setup (multiarch `libdbus-1-dev:arm64` would be needed). Musl it is
   — same posture as totem: static, BLE off, no dbus linkage.
3. Working recipe (debian:12 container, host-arch, ~2 min build after
   deps):
   - apt: `libclang-dev` (rustables/bindgen), `build-essential`
     (build scripts are host-compiled — forgetting this fails on
     `serde`'s build script), `pkg-config`, `wget`
   - musl.cc `aarch64-linux-musl-cross.tgz` → `/opt/cross`
   - `rustup target add aarch64-unknown-linux-musl`
   - `CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_LINKER=/opt/cross/bin/aarch64-linux-musl-gcc`,
     same for `CC_aarch64_*`/`AR_aarch64_*`
   - **`BINDGEN_EXTRA_CLANG_ARGS_aarch64_unknown_linux_musl="--sysroot=/opt/cross/aarch64-linux-musl"`**
     — without it rustables' bindgen dies on `asm/types.h not found`
     (no target headers on the include path)
   - `cargo build --release --locked --target aarch64-unknown-linux-musl`
   - Output: static aarch64 ELFs (fips, fipsctl, fipstop, fips-gateway)

### Deploy

Mirrored totem's layout exactly (it was a manual install, not a .deb):
`/usr/local/bin/{fips,fipsctl,fipstop}`, `/etc/fips/fips.yaml` (totem's
verbatim — persistent identity, lan rendezvous on, TUN, DNS, UDP+TCP
transports), unit at `/etc/systemd/system/fips.service`, `fips` group
created with `totem` added before first start (perms correct first
try, unlike 08-19).

Footgun hit: transferred config+unit as ONE concatenated file and
split on-device by line number — the split truncated the yaml mid-file
(`transports:` lost → daemon crash-looped with `no operational
transports`). Fix: separate files. Never concatenate deploy files.

### Mesh formation & verification

- mDNS LAN rendezvous auto-paired motown with **totem** (no manual
  `fipsctl connect` needed) within seconds of first start.
- motown npub: `npub1rx7epldvux4d306aasw0n6y7wn9je0wa3yw4f8djpsw28g8zqzxsgg2g35`,
  mesh addr `fd71:93b2:349f:afed:9b57:336c:1a1e:871f`.
- Verified: `fipsctl show peers` mutual on both devices; `.fips` DNS
  resolve from totem ✓; mesh ping 3/3, 0% loss, ~21 ms avg (matches
  the 08-19 LAN-vs-mesh baseline); `ssh motown.fips` from the dev
  host over the mesh ✓ (passwordless, host key accepted).
- Mystery nodes: peers include `npub1qrjr...msuc` @ 192.168.8.234
  and `npub10fu6...2cjf` — neither is a totem (likely the dev host's
  own node and something else on the LAN; unconfirmed). Worth
  identifying before demos.
- `fipsctl show status` on motown reports `connection_count: 0` while
  peers show `connected` — field seems to count something else
  (static peer conns?); don't gate health checks on it.

## motown strfry bring-up (aarch64)

Goal: same setup as totem. Source of truth: totem's own deploy —
`~/src/strfry` (no .git), `~/strfry-deploy/` bundle, unit + config.
Key discovery: totem's strfry is **not stock** — it has `mesh` commands
(router/stream/sync) that upstream master lacks. It's the `router`
branch (`5e81e24`) of `references/strfry` — same commit as metot's
lineage. Per the boss: "it's patched for 32 bits, but aside from that
it's just strfry" — the armv6 build needed patches, aarch64 builds
stock.

### Build recipe: `router` branch for aarch64 (docker, emulated)

`docker run --privileged --rm tonistiigi/binfmt --install arm64` first
(reset by reboots), then in an `alpine:3.18 --platform linux/arm64`
container with the Dockerfile's deps:

- **perl deps**: `perl-template-toolkit perl-yaml` (golpe codegen), and
  `Regexp::Grammars` — pure-perl, **not in 3.18**: extract
  `Regexp/Grammars.pm` from an alpine:3.22 container and drop into
  `/usr/share/perl5/vendor_perl/Regexp/` in the builder.
- **flatbuffers must be 23** (generated headers static_assert the major
  version): that pins alpine 3.18 for building (3.22 ships 25).
- Build tree: worktree copy with all `.git` dirs stripped (git
  safe-directory + worktree metadata live outside the mount — simplest
  to remove git entirely; `make setup-golpe` only checks submodule
  presence).
- `make -j4` → 123 MB with debug_info; strip **inside** the container
  (host binutils is x86-only) → 5.1 MB.
- Full emulated build ≈ 6 min wall.

### Deploy (mirrors totem exactly)

Runtime tree = alpine:3.18 aarch64 minirootfs exported from a container
after `apk add lmdb flatbuffers libsecp256k1 libb2 zstd libressl`
(same .so set as totem's `/opt/strfry`). Layout: `/opt/strfry/{bin,lib,
usr/lib,etc/apk}`, config `/etc/strfry.conf` (totem's verbatim —
`bind = "::"` already set), unit `/etc/systemd/system/strfry.service`
with the loader path swapped to `ld-musl-aarch64.so.1`, system user
`strfry`, `install -d -m 750 -o strfry -g strfry /var/lib/strfry`,
`systemctl enable --now strfry`.

Verified: `active`, listening `[::]:7777`; NIP-11 from totem over the
mesh (fd71::… addr) ✓; NIP-11 from host over LAN IPv4 ✓ (`motown.local`
from the host picked a bad AAAA — use the IP or `.fips` name); `mesh`
subcommands present ✓. Both test units now run identical stacks:
fips @ 23ec0a7 + strfry router-branch.

Build artifacts kept under `/var/tmp/strfry-src` (source+log) — /tmp
is wiped on reboot (lost the first attempt mid-build).

## Staleness sweep + motown NIP-11 markers

A parallel session (NIP-11 marker work, journal 08-19 tail) had set
`!Totem totem` + `self` on totem's `/etc/strfry.conf` — after our
config copy raced it, so motown luckily got the clean pre-marker conf
(no identity mixup). Gave motown its own markers per 07:

- `info.name = "!Totem motown"` — verified rendering over the mesh
  from totem (NIP-11 fetch) after restart.
- `info.self = 19bd90fdace1aad8bf5dec1cf9e89e74cb2cbddd891d549db20c1ca3a0e2008d`
  (hex npub, derived on totem via `nak decode <npub>` — the quick
  hand-rolled bech32 decode wasn't worth it). Dormant like totem's
  (build predates the field); picks up on strfry upgrade.
- Follow-up (same day, cross-session decision): the npub claim moved to
  **`info.pubkey`** (present since strfry's initial commit → renders
  now; on a totem the device npub *is* the admin identity per 02).
  Set on motown, verified over mesh: NIP-11 returns the hex. `self`
  stays as a dormant duplicate for post-upgrade self-description.
  Footgun: golpe's config parser takes the last duplicate key — insert
  into the stock `pubkey = ""` line, never a second occurrence.
  (Spec 07 update done in the parallel session.)
- Restart race gotcha: `systemctl is-active` right after `restart`
  can read `inactive` (RestartSec window) — re-check before calling
  it failed.
- Docs de-staled: 08-19 "motown: still no strfry" line annotated,
  strfry-bind gotcha now says totem+metot+motown, loose ends trimmed
to the mystery peers.

### totemd: aarch64 target added (control-plane session)

`aarch64-unknown-linux-musl` joins the totemd cross builds (rust-lld,
`.cargo/config.toml`) — both bench architectures now produce static
totemd binaries from the host, no docker needed (the crate is pure
Rust, unlike fips/strfry). Cross-checked motown's NIP-11 over ssh:
`!Totem motown` + hex pubkey correct; noted `supported_nips` lacks 77
— verify `negentropy` capability there before the sync slice relies on
it (totem/metot report `"negentropy": 1`).

### totemd: standing install (like fips/strfry)

`deploy/systemd/totemd.service` (User=totem — fips-group socket access
is enough today; root comes with the challenge slice, per the unit
comment) + `deploy/flash.sh`: builds per-arch, installs to
/usr/local/bin (totemctl as symlink), unit, enable --now, status check.
Unreachable devices skipped (metot off). totem: installed, enabled,
`totemctl peers` → 2 live peers. motown: binary + unit shipped to /tmp;
install needs interactive sudo (no NOPASSWD there — unlike totem/metot;
flash.sh tolerates it, just run the one-liner manually).

### Fleet symmetry: motown sudo + standing totemd install

Sudo asymmetry explained: totem (and likely metot) carry a cloud-init
artifact — `/etc/sudoers.d/90-cloud-init-users` grants the default user
NOPASSWD:ALL; motown's plain image didn't. Decision: passwordless on
all bench units (single-user toys behind the LAN; fips.key stays
root-only regardless). motown now has `90-totem-nopasswd` (440).

`deploy/flash.sh motown` then ran hands-off: totemd installed,
enabled+active on motown — sees 3 peers (totem, npub10fu6…, npub1qrjr…
— the two mystery LAN nodes persist across devices; still unidentified).
Fleet state: totem + motown running totemd.service with live bus;
metot pending power-on (conventions + install).

## metot catch-up (fleet symmetric again)

metot powered back on. It was behind on exactly two things: NIP-11 markers
and the totemd standing install. fips/strfry/nak were already in place and
healthy (fips connected, `bind = "::"` intact, mesh peer of the mystery
nodes + fleet).

- NIP-11 markers: `name = "!Totem metot"`, `pubkey = 93fad9e4…f27d7f` set
  via sed on the stock lines (last-duplicate-key footgun respected),
  dormant `self` line added like totem/motown. Backup `/etc/strfry.conf.bak-metot`.
  Verified loopback AND from totem over the mesh (`!Totem metot` + hex render).
- totemd: `deploy/flash.sh metot` ran hands-off (NOPASSWD present as
  predicted) — installed, enabled+active, fips connected, 4 peers.

All three units now run the full stack: fips @ 23ec0a7, strfry router
branch with `!Totem` markers, totemd 0.1.0.

## totemd: operator config + cached NIP-11 prefilter

Design boundary pinned before code:

- FIPS owns rendezvous/auto-connect; totemd owns the engagement policy after
  an authenticated peer appears.
- NIP-11 is an unsigned prefilter only. Its per-npub grade MAY be cached;
  the signed challenge verdict remains per-encounter and uncached (02).
- Sync and friendship are independent: sync is relay data exchange; kind 3
  is a published social claim. Factory `befriend = "ask"`; demo units MAY
  use `auto`.

Implemented:

- `/etc/totemd/config.toml` (`TOTEMD_CONFIG` path override), strict TOML:
  missing → defaults; malformed → startup failure. Keys now:
  `net.probe=true`, `net.verdict_ttl_hours=24`,
  `policy.befriend=ask|auto|never`, `policy.sync=true`.
- NIP-11 prober: GET `http://[mesh-ip]:7777/` with the nostr Accept header;
  `!Totem` + `pubkey` (hex or npub) must match the FIPS-authenticated npub.
  Probe grades: candidate / not_totem / unreachable.
- In-memory grade cache: candidates for daemon lifetime; negative/
  unreachable for TTL. No persisted reputation/backoff until measurements
  justify it. End-to-end fake-relay test proves the second encounter causes
  no second HTTP GET.
- Bus: `totem.config.get`; `totem.peers.get` gains `probe_verdict`;
  `totem.peer.candidate` push. `totem.recognized` remains reserved for a
  successful signed challenge.
- deploy template + flash: creates config only when absent (operator edits
  survive upgrades). `totemctl config` added; AGENTS.md records the standing
  fleet paths and inspection commands.
- Specs 02/03/04/07/08/10 updated for cache boundary, engagement policy,
  sync≠friendship, candidate event, and S3's explicit auto-befriend setting.
  04 records bench strfry `router` branch `5e81e24` as deployment detail.

Checks: 18 tests green (pure parsing/cache + real fake relay socket), clippy
`-D warnings`, static armv6-musl + aarch64-musl builds.

### Fleet proof

Flashed current build/config to totem + motown; metot had returned (parallel
session had already set markers/install), so flashed it too. All three:
service active, effective defaults correct, 4 direct FIPS peers. Each grades
the other two fleet npubs `candidate` and the two ordinary mesh peers
(`npub10fu6…`, `npub1qrjr…`) `unreachable`; each emitted exactly 4 seen +
2 candidate pushes. This is the first real symmetric totem-recognition
prefilter across the three-device fleet.

Performance snapshot after 5–7 min with 4 direct peers: totem RSS 2.6 MiB /
0.3% CPU, metot 2.6 MiB / 0.1%, motown 1.8 MiB / 0.1%; static binaries
4.1 MiB armv6 and 4.4 MiB aarch64. Probe work stayed one-shot (4 seen,
2 candidate; no poll-loop event growth).

### Signing dependency spike

The next challenge slice should use core `nostr`, not full `nostr-sdk`:
core supplies NIP-19/bech32 + canonical events + Schnorr signing/verifying;
the SDK's relay pool/client is redundant (strfry owns relay operations).
Scratch armv6 build confirmed `nostr 0.45.3` pulls secp256k1 C and fails our
rust-lld-only cross path without `arm-linux-musleabihf-gcc`. Decision:
keep tiny `bech32` only for this prefilter; challenge adds the proven
musl.cc cross-C toolchain, adopts core `nostr`, replaces the helper and drops
direct bech32. Do not hand-roll event signing/canonicalization to save a
build dependency.

## totemd: signed challenge (totem ↔ motown scope)

Scope constraint from operator: metot is in parallel peripheral work. No SSH,
flash, restart, file/config change, or peripheral action targeted metot in this
slice. Since it remained a direct NIP-11 candidate, the normal net loop sent
its read-only challenge GET; its older daemon has no responder, so it remains
`candidate`, `recognized=false`. All active deployment/inspection commands
named only `totem motown`.

### Protocol and privilege decisions

- Adopted core `nostr 0.45.3` (`std` + `os-rng`), not `nostr-sdk`: canonical
  event ID, Schnorr signing/verification, NIP-19 parsing. Removed temporary
  direct bech32. The credential read buffer is `Zeroizing`; `nostr::Keys`
  zeroizes its secret on drop.
- Kept `totemd.service` as `User=totem`. systemd 257 `LoadCredential=` reads
  `/etc/fips/fips.key` (source stays root:root 0600) and exposes a private
  read-only credential. Verified first with transient units on totem+motown,
  then live (`User=totem`, `NoNewPrivileges=yes`, source mode still 600).
- No wall-clock acceptance window. Offline Pis cannot assume RTC/NTP after a
  cold boot; a fresh random 128-bit nonce, used for one request then discarded,
  supplies replay resistance. A test signs at `created_at=1` and still proves.
- Responder signs exact kind 27235 / empty content / nonce + endpoint URL + GET
  tags; strict verifier checks event ID, Schnorr signature, FIPS npub match,
  exact shape, and all bindings. Response is `Cache-Control: no-store`.
- Guest-reachable signing is capped by a global 8-signature/second burst
  bucket. This admits simultaneous legitimate peers while bounding armv6 work;
  per-source buckets are deferred unless measured guest abuse causes starvation.
- Recognition is a current-encounter set: successful proof pushes
  `totem.recognized` once; FIPS `gone` clears it. Each challenge captures the
  peer's `first_seen` encounter token, so a slow proof cannot cross a rapid
  disconnect/reconnect. NIP-11 candidate caching remains separate and survives
  encounters for the daemon lifetime.

### Build and control surface

The secp256k1 C build did not need downloaded musl.cc toolchains. Existing
Clang works with its builtin freestanding headers; `.cargo/clang-musl` adds
ARMv6 hard-float/VFPv2 flags and Cargo still links with `rust-lld`. Verified
ELF attributes: ARM EABI5, CPU v6, VFPv2, VFP-register ABI; aarch64 static too.
Release `strip=true` keeps binaries at 4.1 MiB armv6 / 4.3 MiB aarch64 despite
adding canonical crypto.

Shared bounded HTTP code now covers NIP-11 and challenge GETs (5s timeout,
64 KiB cap, correct IPv4/IPv6 URL form). `totem.peers.get` reports
`probe_verdict`, bounded unsigned `nip11_name`, and `recognized`; status
reports recognized count. FIPS state
is installed before seen pushes/tasks, fixing the old tiny stale-reconciliation
window. `totemctl` gained help/-h/--help, version, recognition descriptions,
and clean EPIPE behavior; the flash script no longer pipes status through
`head`. Ports pinned in spec 07: relay 7777, web/challenge 8080, bus 8081.

### Live proof and IPv6 finding

First deployment correctly loaded both signing keys but challenge connects got
RST. Root cause was the same class as strfry: web default `0.0.0.0:8080` was
IPv4-only while FIPS is IPv6-only. Changed default to `[::]:8080` (dual-stack
on these images). Totem also had a stale explicit
`TOTEMD_WEB_ADDR=0.0.0.0:8080` in `/etc/totemd/totemd.env`; migrated only that
scoped unit to `[::]:8080`. AGENTS now records the mesh-facing bind gotcha.

Final exact stripped build flashed to totem+motown only. Both listen on
`*:8080`; each reports 4 FIPS peers, 2 candidates, exactly one
`totem.recognized`, recognized count 1. Totem marks only motown recognized;
motown marks only totem recognized. Their recognized rows cache the existing
probe response as `nip11_name = "!Totem motown"` / `"!Totem totem"`; npub
remains the authenticated identity. Names are capped at 128 bytes and rejected
if they contain terminal controls, with no additional HTTP request. Direct
motown→totem proof returned 200,
`no-store`, kind 27235, totem's exact hex pubkey, and exact nonce/u/GET tags.
After ~90s: RSS 3.6–3.9 MiB, CPU snapshot 0.2–0.5% during early uptime; event
counts stayed fixed (no retry/poll storm).

### Final consistency / correctness / ponytail pass

Found and fixed three pre-commit correctness edges: a slow proof could have
crossed a disconnect/reconnect; `PublicKey::parse` accepted NIP-21/nprofile
forms beyond 07's hex-or-npub rule; spawned web/bus task errors were ignored.
Recognition now atomically checks the captured encounter token; one shared
parser restricts both NIP-11 claims and challenge expectations to hex/npub;
`try_join!` makes either server failure terminate for systemd restart. Invalid
bind env values now fail startup instead of silently exposing the default bind.

Ponytail cuts: deleted the one-line claim parser and unused public surface,
removed a snapshot clone and server task wrappers, and changed flash from
`enable --now` + `restart` (two first-install starts) to `enable` + one
`restart`. Final flash produced exactly one daemon start per scoped device;
both still mutually recognize with the expected `nip11_name`; local and remote
binary checksums match. Logging now follows terminal detection, leaving
journald with zero ANSI bytes. No remaining speculative abstraction/dependency
was found: core nostr, zeroizing credential, shared bounded HTTP helper, and the
small global limiter each have two or more concrete jobs or a security boundary.

Checks: 23 tests (strict/tampered/clock-independent proof, burst limit,
real-socket responder→prover→state/push, stale-encounter rejection,
bounded/safe NIP-11 hint cache), clippy
`-D warnings`, fmt, shell syntax, diff check, local full-daemon smoke,
armv6+aarch64 static release builds, and two-device live proof.
