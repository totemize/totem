# 2026-08-21

## Waveshare V4 contrast and flash correction

Compared Totem's V4 partial-refresh sequence directly with Pwnagotchi commit
`6e31b26` on the only authorized display host, metot. Pwnagotchi defaults to
event-driven UI refresh, establishes one full base image, then pulses reset for
1 ms and writes only the new `0x24` plane for each `0xFF` partial update. Totem
was instead retaining reset across a burst, rewriting the prior frame to
`0x26`, and promoting every twentieth requested partial update to a disruptive
full refresh.

A deterministic camera-visible test alternated filled and outlined blocks for
41 updates under each driver sequence. Both paths took about 26 seconds. The
old Totem sequence left the nominally white block gray and fine text weak. In
the same raw camera crop, the median white-minus-black luminance separation was
36 levels. The Pwnagotchi sequence produced a clean white field, dense black,
and readable fine text, with 47 levels of separation (31% larger).

The V4 production driver now pulses reset before every partial frame and writes
only `0x24`. A full refresh still seeds both RAM planes when controller state is
unknown, and any I/O failure still invalidates the baseline. Scheduled full
promotion is disabled by default with `TOTEM_EINK_FULL_REFRESH_EVERY=0`; a
positive deployment value remains available for panels that need an explicit
cadence. Metot's converged Ansible inventory pins `0`.

Deployed the content-addressed Python release only to metot. Ansible completed
with `ok=105 changed=12 failed=0 unreachable=0`, and all five services plus SPI,
I2C, UPS, relay, control-plane, and challenge checks passed. A production-path
stress run then completed 201 partial updates in 127.036 seconds without a
full-panel flash. The final raw camera crop retained 49 luminance levels of
white/black separation, slightly above the 47-level result after 41 updates,
so contrast did not decay over the run. The screen and device-manager services
were restored active with cadence `0`; the live Totem face, rules, lightning
badge, and footer were camera-readable.

Local gates: 217 Python tests passed with 5 hardware-only skips; changed Python
files passed Black; compileall, `git diff --check`, and Ansible syntax check
passed. The `spec/` tree was not modified.

After formatting the manager, a final metot-only converge installed exact
release `sha256-bfdbe1144294204abab9cae4640103c8763635cb228b37b6619b635f04e44f11`.
It completed at `ok=105 changed=9 failed=0 unreachable=0`; all verifier gates
passed and both display services were active.

## Authoritative interactive face animations

Expanded the post-boot renderer from simple loops into interruptible animation
within the currently authoritative scene. Snapshot reconciliation and priority
arbitration still choose the semantic state; the animator only advances frames
inside that choice. A higher-priority scene preempts the current motion, and a
candidate animation is keyed to its encounter, plays the blush/wink sequence
once, then holds the final blush until that encounter changes.

Added the approved idle look-around, charging expression loop, bidirectional
sync transfer, suspicious non-Totem glasses, and second candidate flirt
sequence. Charging reactions are bounded and injectable: a centered frame can
blink for two seconds at 20% or glance for four seconds at 10%, never twice in
a row. The first authoritative plugged-in rise to 100% shows a retry-safe
ten-second star face once, then rearms only after dropping below 100%. Replay is
exhaustive rather than random, so it includes both optional reactions and the
full-charge frame.

All added glyphs are closed-set vector art. Every frame fits the 250×122 1-bit
canvas while header/footer bytes remain fixed. The non-Totem sequence received
a final physical-layout correction: all seven frames use one fixed left-side
origin and scale, so the face stays still while glasses enter, settle, question,
and leave.

Local gates finished with 249 Python tests passing and five hardware-only
skips, plus 50 Rust tests, strict clippy, Rust format, Black, flake8 with the
repository's E501/W503 exclusions, compileall, diff check, and Ansible syntax.
The `spec/` tree remained untouched. After the physical replay, a final
metot-only converge included the corrected root deployment README in exact
release `sha256-0ca38fc189297b67e0daffa28eadd743c3ef499bc8f3a7f7cc2a92643abc256c`.
It completed with `ok=105 changed=9 failed=0 unreachable=0`; full-refresh
cadence remained zero. A second converge reused that exact release and finished
`ok=100 changed=0 failed=0 unreachable=0`.

A deterministic 49-frame replay then ran on the physical V4 panel. The first
frame used the safe full path and all later frames requested partial refresh.
The complete camera capture, exact device atlas, overall contact sheet, and a
seven-frame suspicious-sequence sheet are recorded in
[`../proofs/2026-08-21/interactive-animations/`](../proofs/2026-08-21/interactive-animations/README.md).
The panel stayed crisp without tofu, clipping, periodic full flashes, or face
drift. Both display services were restored active after replay.

## Authoritative footer note count

Replaced the recognized-friend star badge with the requested vector `[•]` mark
and added a folded-paper note glyph with a right-aligned kind-1 note count. The
new count is not presentation guesswork: `totemd` runs a bounded, unprivileged
`strfry scan --count '{"kinds":[1]}'` query every 15 seconds and exposes the
cached result as `totem.status.get.status.notes`. A failed or not-yet-completed
query is `null`, which the screen renders as `?` rather than fabricating zero.

Local gates passed with 250 Python tests and five hardware-only skips, 53 Rust
tests in the deterministic sequential gate, strict Clippy, Rust format, Black,
flake8, compileall, diff check, and Ansible syntax. The existing sync child
timing tests each passed repeatedly in isolation; the parallel Rust run retained
its known loaded-runner timing sensitivity, so the sequential complete suite is
recorded as the deterministic gate. The `spec/` tree remained untouched.

Deployed exact Python release
`sha256-bcca9712b2e012a379bcbbb1e84dbd9e18859a70068cd0930795a90c0ef1f238`
only to metot. Live `totemctl status` reported `notes: 404`; `totemd`,
`totem.service`, and `totem-screen.service` were active. Desk View inspection of
the physical V4 panel confirmed the `[•]` friend mark and the isolated paper-note
`404` badge were crisp and unclipped. The first deployment completed with
`ok=106 changed=11 failed=0`; the second reused the exact release and converged
with `ok=100 changed=0 failed=0`.

## State-specific word-reveal captions

Added a presentation-only caption animator on top of the authoritative runtime
scene projection. Each of the 13 scenes owns exactly ten approved phrases. A
chooser is injected for deterministic tests, selects once per genuine scene
admission, excludes that scene's immediately previous phrase, and keeps the
selection stable across face frames, polling, SSE wake-ups, charging reactions,
and display retries. Captions never participate in scene priority, dwell,
encounter identity, or one-shot delivery.

The selected phrase begins at its first word and accumulates one word at a time
near the 1.2-second target. Caption and face deadlines are independent and the
runtime wakes for whichever becomes due first. Transfer-aware scheduling gives
the initial full-refresh prefix a readable settled hold; later prefixes request
partial refresh without changing the face step. A higher-priority authoritative
scene still preempts both animations immediately.

Reserved a fixed 12-pixel caption band between the character and footer, with a
three-pixel clear gap above the unchanged footer rule. Every approved phrase
fits on one line in the 9-pixel bold font. Each progressive prefix is anchored
at the final phrase's centered origin, so existing words do not jump. The
renderer rejects missing glyphs, clipping, or band violations. The deterministic
`totem-screen proof-captions` command renders a 229-frame mode-1 atlas covering
all 130 complete captions, representative prefixes for every scene, and every
face sequence without issuing a long physical replay.

Local verification passed all 261 Python tests with five hardware-only skips,
Black, changed-file flake8, compileall, diff check, and Ansible syntax. No Rust
code changed and the `spec/` tree remained untouched.

Deployed exact release
`sha256-ae10b450ccc3fa94f7208ed1bb7c1482cc7c73a73e39447e171e4cee73a96a33`
only to metot. A controlled camera run captured `drinking` → `drinking
organized` → `drinking organized lightning.` while the face, header, footer,
`[•]`, paper-note glyph, and live `404` count remained fixed. The first prefix
received a readable full-refresh hold; subsequent completions were about 1.29
seconds apart and stayed crisp with cadence zero and no periodic flash. The
complete atlas, video, contact sheet, timing evidence, and hashes are recorded
in [`../proofs/2026-08-21/state-captions/`](../proofs/2026-08-21/state-captions/README.md).
A second metot-only convergence reused the exact release and completed with
`ok=100 changed=0 failed=0 unreachable=0`.

## Totem display enablement

Enabled the same Waveshare 2.13-inch V4, 250×122, 180-degree display profile on
the older `totem` host. Hardware inspection confirmed a Raspberry Pi Zero W
Rev 1.1 (`armv6l`) with existing SPI nodes; metot remains the faster Zero 2 W.
Only the confirmed screen/SPI profile was enabled—totem's UPS/I2C configuration
remains unset.

The first fresh enablement exposed an Ansible ordering bug: the managed
environment already selected `waveshare_2in13_v4`, but the screen was started
before the pending device-manager restart ran. The old manager process retained
an empty driver selection, auto-detected the legacy 3.7-inch default, and the
screen request timed out. Moved the handler flush ahead of screen startup so a
freshly enabled screen always talks to the new release and environment. The
failed attempt stopped at the display gate; no false-success verification was
possible.

After the stale hardware worker reached its bounded systemd shutdown, the
manager restarted with the V4 driver and exact release
`sha256-ae10b450ccc3fa94f7208ed1bb7c1482cc7c73a73e39447e171e4cee73a96a33`.
Camera inspection showed the live `totem` idle face, progressive caption,
header, `[•]` badge, and `404` note count cleanly on the right-hand panel. The
whole device is angled in the camera frame; its content orientation is correct
relative to the enclosure. `totemd`, `totem.service`, and
`totem-screen.service` were active, and the repaired full totem-only converge
completed `ok=96 changed=0 failed=0 unreachable=0` with all verifier gates.

## Neutral idle versus lonely idle

Live metot evidence exposed an unconditional presentation fallback: the device
reported mesh size 1298, four present peers, and two present recognized friends,
but after one-shot recognition and sync payoffs were consumed the projector
still selected `alone_idle` and lonely copy such as “counting invisible
guests.” The wider mesh size remains intentionally irrelevant to direct social
presence; the authoritative input is each live `totem.peers.get` row's
`recognized` flag, from which the footer friend count is also derived.

Added a distinct neutral `idle` runtime scene. It uses the approved calm
look-around animation and ten non-lonely captions whenever at least one present
peer is recognized. `alone_idle` remains the fallback after all recognized
friends depart. Peer, sync, power, and mesh-health scenes keep their existing
priority, so on plugged-in metot the live candidate set was `charging, idle`
and charging correctly remained visible while `idle` replaced the old social
fallback underneath it.

The exact reported four-peer/two-friend case is covered by regression tests,
along with departure back to `alone_idle`, startup seeding, bounded payoff
queues, the closed scene/caption catalog, renderer bounds, Gherkin bindings,
and deterministic proof output. The catalog now contains 14 scenes, 59 face
frames, 140 captions, and a 253-frame caption atlas. Local verification passed
273 Python tests with five opt-in hardware tests skipped, changed screen files
passed Black and the repository's E501/W503 flake8 exclusions, compileall and
diff check passed, and Ansible syntax plus the metot-only host limit passed.

Only metot was used for device testing. Exact release
`sha256-2f1df21d17176e15dc801b0ce73eeeaf12bdd9d57bb790238b2d227aaf04df68`
deployed with `ok=105 changed=9 failed=0`; the on-device live snapshot projected
the recognized-peer fallback as `idle`, the 253-frame proof rendered, and
`totemd`, `totem.service`, and `totem-screen.service` were active. A second
metot-only converge reused the release and completed
`ok=100 changed=0 failed=0 unreachable=0`. The `spec/` tree was not modified.
## Two-device Wi-Fi fallback acceptance

Merged current `origin/master` (`93e45fc`) into `feat/wifi-ap-fallback` as
`9f607b1`, preserving both journal histories. The integrated tree passed 101
Python tests, 40 Rust tests, three webapp Node tests, Svelte check, Vite build,
Ansible syntax, ShellCheck, shell syntax, offline NetworkManager profile
parsing, and diff checks. The merge remains local pending push approval.

Preflight targeted only totem and motown. Both were healthy on
`netplan-wlan0-NOBIOS`, with NetworkManager 1.52.1, AP-capable 2.4 GHz radios,
active FIPS/strfry/totemd, active FIPS TUNs, no failed systemd units, ample
storage/memory, and bidirectional recognized/synchronized FIPS state. No
`!Totem` BSS was visible. Motown already matched the source fallback hashes;
totem did not have the feature. The network role was installed on totem only
(`changed=7 failed=0`) and immediately converged (`changed=0`) without changing
its infrastructure connection. An explicit oneshot run on each device logged
that the active infrastructure profile was left unchanged.

Every disruptive round first disabled only the active infrastructure profile's
autoconnect and armed an independent transient restore timer. Restoration used
`/bin/sh` or `/usr/bin/nmcli`, never direct execution from the noexec `/run`.
No command, deployment, or SSH session targeted metot; it appeared only as an
ordinary passive FIPS peer before the isolated rounds.

### Motown host, totem peer

Motown left infrastructure at 23:58:16. After reconnect grace, jitter, and its
final scan, fallback selected AP mode at 23:58:42 and activated the open
`!Totem` BSS (BSSID `2C:CF:67:A3:EB:E0`) at 23:58:43. Totem then selected
`totem-peer`; motown's DHCP log proves lease `10.21.0.46` for hostname `totem`
and MAC `b8:27:eb:0f:93:5c` at 23:59:51. The workstation also joined and
received `10.21.0.75`, proving multi-client DHCP and no client isolation.

Motown discovered and cryptographically authenticated the exact totem npub at
`10.21.0.46:2121`, promoted it to an active FIPS peer, and established the
session. Totemd recognized the signed device identity and completed three
five-minute rounds over the isolated link, each `Set reconcile complete. Have
0 need 0`. FIPS, strfry, and totemd stayed active. Timed restoration returned
both devices to infrastructure with autoconnect enabled.

Joining the isolated AP from the development workstation also proved the user
path but severed the agent's control transport, producing repeated `fetch
failed` errors until the workstation's own delayed restore fired. Subsequent
rounds kept the workstation on infrastructure and captured evidence locally on
each device instead.

### Totem host, motown peer

Totem left infrastructure at 09:10:58. Fallback selected AP mode at 09:11:23
and completed activation at 09:11:28 with BSSID `B8:27:EB:0F:93:5C` and the
configured `10.21.0.10`–`10.21.0.209` pool. Motown left infrastructure at
09:12:03, logged `joining existing !Totem network` at 09:12:20, activated
`totem-peer` at 09:12:23, and received `10.21.0.206`. Thirty-five workstation
scans observed only the totem BSSID; no split network formed.

Motown discovered the exact totem identity at `10.21.0.1:2121`; totem saw the
exact motown identity at `10.21.0.206`. Both established FIPS sessions,
completed signed recognition, and reconciled `Have 0 need 0` with exit 0.
Device-local sampling found FIPS, strfry, and totemd active throughout and
successfully fetched totem's NIP-11 document directly from `10.21.0.1:7777`.
The independent timers restored motown first and totem second.

### Simultaneous infrastructure loss

Both disconnect timers fired at the same second, 09:21:07. Motown's fallback
started at 09:21:11, selected AP mode at 09:21:28, and activated it at
09:21:29. Totem's fallback started at 09:21:31, observed the new AP on its
first post-grace scan, logged `joining existing !Totem network` at 09:21:50,
and completed `totem-peer` DHCP at 09:21:56 with `10.21.0.46`.

Across 27 observations from 09:21:36 through 09:25:14, the workstation saw
exactly one `!Totem` BSSID (`2C:CF:67:A3:EB:E0`). Device-local captures then
proved bidirectional FIPS authentication specifically over
`10.21.0.1:2121`/`10.21.0.46:2121`, repeated successful direct relay/NIP-11
access, signed recognition on both sides, and two successful periodic `Have 0
need 0` rounds. All three core services remained active in every sample.

The single synchronized trial confirms the intended outcome on these two
radios, but randomized backoff remains probabilistic rather than a formal
leader-election guarantee. More repetitions are needed before assigning a
split-rate; no split was observed in this acceptance.

### Final state and conclusion

Test-only scripts, captures, transient units, timers, and the workstation
profile were removed. A final role run limited explicitly to `totem:motown`
converged both devices at `changed=0`. Both now have source-identical fallback
artifacts, inactive/non-autoconnecting AP and peer profiles, enabled fallback,
and infrastructure autoconnect restored. Totem is back at `192.168.8.136`,
motown at `192.168.8.196`, and the workstation at `192.168.8.165`; no `!Totem`
BSS remains. Both report FIPS `running`, TUN `active`, exact LAN peer transport,
zero decrypt failures, signed recognition, successful sync, active core
services, and zero failed units.

Each radio logged three `brcmf_vif_set_mgmt_ie: vndr ie set error: -52` lines
while changing roles. NetworkManager nevertheless completed every AP, peer,
and infrastructure activation; DHCP, FIPS, relay sync, and restoration all
succeeded. Treat this as a Broadcom transition warning to monitor, not a failed
acceptance.

Conclusion: infrastructure preservation, automatic AP hosting, automatic peer
joining, the fixed gateway/DHCP contract, multi-client access, FIPS identity,
signed Totem recognition, relay reconciliation, role symmetry, synchronized
outage recovery, sticky isolated operation, and safe infrastructure restoration
all behaved as designed on totem and motown. The remaining known ceilings are
the deliberately open demo network, probabilistic simultaneous election, and
the need for device-local capture whenever the controller requires its
infrastructure connection.

## Empty-AP recovery

Replaced the intentionally terminal sticky-AP rule with a conservative idle
retry. A new one-minute systemd timer invokes the existing fallback action in
check-only mode. While `totem-ap` is active, `/usr/sbin/iw dev wlan0 station
dump` is the single source of associated clients: any station (Totem, human
FIPS client, laptop, or phone) preserves the AP. Ten uninterrupted empty
minutes deactivate the AP and restart the existing infrastructure → visible
`!Totem` → AP selection cycle. The idle start is a monotonic `/proc/uptime`
value under `/run`, so wall-clock correction is irrelevant and reboot clears
it. Failed connection, station, or uptime inspection removes the marker and
preserves the AP rather than risking a client disconnect. `iw` is the only new
runtime package; no daemon or TCP/FIPS-specific activity tracker was added.

Local checks passed: 102 deterministic Python tests (including empty start,
station reset, expiry, and inspection-failure behavior), Black, Flake8,
ShellCheck, shell syntax, offline NetworkManager profile parsing, all three
systemd units, direct YAML parsing, Ansible syntax/list-tasks, and diff checks.
The network-only role installed the script, idle oneshot/timer, and `iw` on
exactly totem and motown, then converged both at `changed=0`.

A motown-only empty-AP test shortened the threshold to 60 seconds through a
runtime systemd drop-in. Motown activated `!Totem` at 10:36:39 with no clients;
the timer marked it empty at 10:37:00 and deactivated it at 10:38:10, then the
existing fallback action ran and rehosted at 10:38:38. Further accelerated
checks repeated the same cycle at 10:40:20, 10:42:40, and 10:44:50. It rehosted
because the harness's explicit `nmcli connection down` places NetworkManager's
infrastructure profile under a manual autoconnect block until an explicit
`connection up`; this is a harness property, not fallback selection failure.
The independent restore returned motown to `NOBIOS` at 10:45:55.

A second accelerated run closed that harness gap with a temporary clone of the
same infrastructure profile which was not manually blocked and became eligible
only after `totem-ap` was active. Motown hosted at 11:14:44, marked the empty AP
at 11:15:30, and deactivated it at 11:16:40. NetworkManager auto-activated the
newly eligible infrastructure profile at 11:16:41; the existing fallback
oneshot observed `infrastructure reconnected during grace period` at 11:16:56
and did not rehost. The original profile was then activated and the clone was
deleted. This proves the complete empty AP → infrastructure recovery path, not
only AP recycling.

The associated-client guard then used the same 60-second threshold. Motown
hosted at 10:55:59. Its first empty check started the marker at 10:57:00;
totem joined `totem-peer`, received `10.21.0.46`, and appeared in the kernel
station table as MAC `b8:27:eb:0f:93:5c` with increasing traffic. At 10:58:10
the idle service explicitly logged `AP station connected; resetting idle
timer`. Later checks at 10:59:20, 11:00:30, and 11:01:40 preserved the same AP;
24 workstation scans saw one unchanged BSSID for more than three minutes.
FIPS authenticated the exact identities and the encounter reconciled `Have 0
need 0`. One pre-encounter sync exited 1 while the deliberate infrastructure
link was disappearing; the isolated encounter and restored-LAN rounds both
succeeded immediately afterward.

All shortened-threshold drop-ins, transient timers/services, captures, idle
markers, and test profiles were removed. Production is back to 600 seconds.
Both devices are on `NOBIOS` with autoconnect enabled, idle timers
`enabled/active`, source-identical script hash
`5166f13e5b8258d599a2904abeb2105f52ae6fb9a4a46e6710cefc75b77ff7e7`,
active NetworkManager/FIPS/strfry/totemd, active FIPS TUNs, and zero failed
units. Metot was neither contacted nor changed. The implementation and this
record remain uncommitted and unpushed pending operator approval.
